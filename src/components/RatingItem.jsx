import React from 'react';
import { Link } from 'react-router-dom';


const RatingItem = (size, color) => {
    const style ={
        width: '1rem',
        height: '1rem',
        backgroundColor: "red",
        marginRight:'0.5rem',

    }

  return (

    <div style={style}>
        
    </div>
  );
};

export default RatingItem;
