import React from 'react';
import { Link } from 'react-router-dom';

const Item1 = (size, color) => {
  const style = {
    width: '10rem',
    height: '10rem',
    backgroundColor: 'khaki',
  };

  return <div style={style}>
    
  </div>;
};

export default Item1;
