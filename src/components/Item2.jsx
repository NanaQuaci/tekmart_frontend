import React from 'react';

const Item2 = (size, color) => {
  const style = {
    width: '5rem',
    height: '5rem',
    backgroundColor: 'coral',

    // marginRight: '1rem',
  };
  return <div style={style} className="mx-3">

  </div>;
};

export default Item2;
